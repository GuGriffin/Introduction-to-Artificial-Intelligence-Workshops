x = ['a', 'abc', 'ab', 'abcd']  # Create a list and assign values
x.sort(key=len, reverse=True)    # Sort the elements by their length in descending order
print(x)                        # Print the sorted list
