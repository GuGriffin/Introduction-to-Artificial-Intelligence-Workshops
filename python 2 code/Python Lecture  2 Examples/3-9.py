for n in range(1, 21):  # Loop with `n` taking values from 1 to 20
    if n % 2 == 0:  # Check if `n` is an even number
        continue  # Skip the rest of this iteration if `n` is even
    else:  # If `n` is odd
        print(n)  # Print the value of `n`
