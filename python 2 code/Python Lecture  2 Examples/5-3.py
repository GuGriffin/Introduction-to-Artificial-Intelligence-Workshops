animal = ['elephant', 'monkey', 'snake', 'tiger']  # Create a list called 'animal'
# Iterate over each item in the 'animal' list
for name in animal:
    print(name)  # Print each 'name' (each element in the list)
